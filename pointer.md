## what is pointer?

pointer is a vairable that is stores the memeory address of another variable instead of storing a value directly

a pointer is the address of where something stores or lives in memory


imagine your school has lockers.your homework is inside loocker /42

you can either:
*Give your friend a photocopy of your homework (a copy - she edits hers, yours stays the same)
*Give your friend locker number $2(a pointer - she goes to the real thing and can change it)

that's all a pointer is -- The address of where something lives in memory



